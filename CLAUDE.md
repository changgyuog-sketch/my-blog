# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
글쓰기는 마크다운으로 하고, 빌드 스크립트가 이를 HTML로 변환해 정적 사이트를 생성합니다.

핵심 요구사항:
- 디자인은 **깔끔하고 읽기 좋게** (타이포그래피, 여백 중심의 미니멀한 블로그 스타일)
- **다크모드** 지원 (시스템 설정 자동 감지 + 수동 토글)
- **모바일 반응형** (좁은 화면에서도 읽기 편해야 함)
- 배포되는 결과물은 **프레임워크 없는 순수 HTML/CSS/JavaScript**만 사용 (React, Vue, Next.js 등 프론트엔드 프레임워크 금지)

## 아키텍처

마크다운 → HTML 변환은 **빌드 타임**에 수행합니다 (런타임에 브라우저가 .md를 fetch/파싱하지 않음).
빌드 스크립트 자체는 Node.js로 작성하지만, 이는 개발/빌드 도구일 뿐이며 최종 산출물(dist/ 이하)은 정적 HTML/CSS/JS 파일입니다.

```
my-blog/
├── content/
│   └── posts/           # 원본 마크다운 글 (파일명 예: 2026-08-26-title.md)
├── templates/            # HTML 템플릿 (post, index 등) — 문자열 치환 방식, 별도 템플릿 엔진 미사용
├── assets/
│   ├── css/
│   │   └── style.css     # 전체 스타일 + 다크모드 (CSS 변수 + prefers-color-scheme)
│   └── js/
│       └── main.js       # 테마 토글, 모바일 메뉴 등 최소한의 클라이언트 JS
├── build.js              # 빌드 스크립트 (marked로 md → HTML 변환, 목록/개별 글 페이지 생성)
├── package.json          # devDependency: marked (마크다운 파서)
└── dist/                 # 빌드 결과물 (배포 대상, 순수 정적 파일)
```

- **마크다운 파싱**: `marked` 라이브러리 사용 (빌드 타임에만 devDependency로 사용, 브라우저에 번들되지 않음).
- **프론트매터**: 각 포스트 상단에 YAML 프론트매터로 `title`, `date`, `tags`, `description` 등을 기록. 별도 파서가 필요하면 가벼운 라이브러리(`gray-matter` 등)를 쓰거나 직접 파싱.
- **라우팅**: 별도 서버/라우터 없이 `dist/posts/<slug>/index.html` 형태의 정적 경로 생성.
- **클라이언트 JS는 최소한으로**: 다크모드 토글, 모바일 네비게이션 토글 정도만. 페이지 렌더링 자체는 빌드 타임에 끝나 있어야 함.

## 빌드/개발 명령어

> 아직 초기 세팅 전이므로 실제 스크립트는 구현 시점에 맞춰 채워야 합니다. 아래는 목표 형태입니다.

```bash
npm install       # marked 등 빌드 의존성 설치
npm run build      # content/posts/*.md → dist/ 에 정적 HTML 생성
npm run dev        # 로컬에서 dist/ 를 정적 서버로 띄우고 파일 변경 감지 (선택)
```

## 디자인 가이드라인

- 본문 가독성이 최우선: 적절한 줄 간격(1.6~1.8), 본문 폭 제한(약 65~75자 기준, `max-width` 활용), 여백 넉넉하게.
- 색상/타이포그래피는 CSS 변수(`:root`)로 정의하고, 다크모드는 `prefers-color-scheme: dark` 미디어 쿼리 + `data-theme` 속성 토글로 수동 전환도 지원.
- 모바일 우선(mobile-first) 또는 최소한 완전한 반응형: 브레이크포인트 기준으로 폰트 크기·여백·네비게이션(햄버거 메뉴 등)을 조정.
- 불필요한 장식/애니메이션은 지양하고, 텍스트와 코드블록이 잘 읽히는 것을 최우선으로 함.
- 코드블록은 `marked`의 기본 출력을 기준으로 하되, 필요시 문법 강조는 가벼운 방식(예: 순수 CSS 기반 또는 아주 작은 하이라이터)으로만 추가하고 무거운 라이브러리는 지양.

## 코딩 컨벤션

- 외부 프레임워크/번들러(webpack, vite 등) 없이 순수 `<script>`/`<link>` 태그로 CSS/JS 로드.
- JavaScript는 ES6+ 문법을 사용하되 빌드(트랜스파일) 과정 없이 브라우저에서 바로 동작해야 함.
- 빌드 스크립트(`build.js`)와 브라우저에서 실행되는 코드(`assets/js/*.js`)를 명확히 구분한다. 브라우저 코드에는 Node.js 전용 API(`fs`, `path` 등)를 절대 넣지 않는다.
- 새 포스트 추가 = `content/posts/`에 마크다운 파일 하나 추가 + 빌드 재실행이 전부여야 한다.
